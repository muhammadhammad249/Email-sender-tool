# Environment variables for Frontend

# In local development: points to local backend
# In Vercel production: points to your deployed backend OR leave empty to use relative /api/ rewrites
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Public app URL (used for OpenGraph)
NEXT_PUBLIC_APP_URL=http://localhost:3000
